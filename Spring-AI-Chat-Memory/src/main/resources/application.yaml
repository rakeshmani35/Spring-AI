spring:
  application:
    name: Spring-AI-Chat-Memory

  ai:
   openai:
     api-key: ${OPENAI_API_KEY}



logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG

server:
  port: 9292